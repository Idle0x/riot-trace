---
tierId: 1
moduleId: 2
lessonId: 8
title: "MODULE 02 BOSS FIGHT"
xpReward: 150
scenario: "Blank terminal. Test your understanding of Memory Allocation and Scope."
startingCode: "// 1. Call getNodeInfo() before defining it.\n\n// 2. Define function getNodeInfo() that logs: 'Network: mainnet'\n\n// 3. Declare a let counter starting at 0, log it.\n\n// 4. Reassign counter to 201, log it again.\n"
validationLogic: "if (code.indexOf('getNodeInfo()') > code.indexOf('function getNodeInfo')) throw new Error('Task 1 Failed: Must call function before definition (Hoisting).'); if (!logs.some(l => l === 'Network: mainnet')) throw new Error('Task 2 Failed: Did not log correct network string.'); if (!logs.some(l => l === '0') || !logs.some(l => l === '201')) throw new Error('Task 3/4 Failed: Counter logic incorrect.');"
---

### EXECUTION REQUIREMENTS
This is a Boss Fight. The terminal is yours. 

Your output must match exactly:
1. Call a function named `getNodeInfo()` **BEFORE** you define it (Demonstrating Hoisting).
2. Define the function `getNodeInfo()`. Inside it, log the exact string: `"Network: mainnet"`.
3. In the global scope (outside the function), declare a `let` variable called `counter` and set it to `0`. Log it.
4. Reassign `counter` to `201` and log it again.

---
tierId: 1
moduleId: 2
lessonId: 8
title: "MODULE 02 BOSS FIGHT"
xpReward: 150
scenario: "Follow the execution requirements precisely to prove your understanding of Memory, Scope, and Hoisting."
startingCode: "// EXECUTE OBJECTIVE\n"
syntaxHint: ""
validationLogic: "if (code.indexOf('getNodeInfo()') > code.indexOf('function getNodeInfo')) throw new Error('Task 1 Failed: Must call function before definition (Hoisting).'); if (!logs.some(l => l === 'Network: mainnet')) throw new Error('Task 2 Failed: Did not log correct network string inside the function.'); if (!code.includes('let counter')) throw new Error('Task 3 Failed: You must declare counter using let.'); if (!logs.some(l => l === '0') || !logs.some(l => l === '201')) throw new Error('Task 3/4 Failed: Counter logic incorrect or not logged properly.');"
---

### EXECUTION REQUIREMENTS
This is a Boss Fight. The terminal is yours. 

Your output and code structure must match exactly:
1. Call a function named `getNodeInfo()` **BEFORE** you define it (Demonstrating Hoisting).
2. Define the function `getNodeInfo()`. Inside it, log the exact string: `"Network: mainnet"`.
3. In the global scope (outside the function), declare a `let` variable called `counter` and assign it to `0`. Log it.
4. Reassign `counter` to `201` and log it again.

Clear the module. Route the XP.
